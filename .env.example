# Database (Neon or any Postgres). Get from https://console.neon.tech
POSTGRES_URL=postgres://user:password@host/dbname?sslmode=require
# Or use DATABASE_URL (same format)
# DATABASE_URL=postgres://...

# Admin login password (optional; if unset, dev token is used)
ADMIN_PASSWORD=your-secure-password

# Public app URL (for redirects and API base). Vercel sets VERCEL_URL automatically.
# Set this if you use a custom domain or need a fixed test URL.
NEXT_PUBLIC_APP_URL=https://gotzportal.vercel.app
